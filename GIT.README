
1. How to commit changes to the master repository?

- git status
this gives you a list of files, that have been changed

- git add [list of files]
this command will add the specified list of files into a commit list

- git commit -m "Message"
this will create a request to push the files from commit list with given message onto the server

- git push
this will actually push the added files onto the server 

2. How to upload the latest revision from the server?

- git checkout

